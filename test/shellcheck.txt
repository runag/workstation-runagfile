
In bin/install-and-deploy line 27:
  fail() {
  ^-- SC2120: fail references arguments, but none are ever passed.


In bin/install-and-deploy line 34:
      sudo apt update || fail
                         ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 35:
      sudo apt install -y git || fail
                                 ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 42:
    cd "${clonePath}" || fail
                         ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 43:
    git pull || fail
                ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 45:
    git clone https://github.com/senotrusov/my-computer-deploy.git "${clonePath}" || fail
                                                                                     ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 46:
    cd "${clonePath}" || fail
                         ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In bin/install-and-deploy line 49:
  bin/deploy || fail
                ^--^ SC2119: Use fail "$@" if function's $1 should mean script's $1.


In deploy-lib.sh line 397:
  . "${output}" || fail
    ^---------^ SC1090: Can't follow non-constant source. Use a directive to specify location.


In deploy-lib.sh line 417:
  . "${output}" || fail
    ^---------^ SC1090: Can't follow non-constant source. Use a directive to specify location.


In data-pi/led-heartbeat.sh line 18:
  for i in {1..5}
  ^-^ SC2034: i appears unused. Verify use (or export if used externally).


In ubuntu/ubuntu-lib.sh line 319:
      echo 'XDG_DESKTOP_DIR="$HOME/Desktop"' >>"${tmpFile}" || fail
           ^-- SC2016: Expressions don't expand in single quotes, use double quotes for that.


In ubuntu/ubuntu-lib.sh line 323:
      echo 'XDG_DOWNLOADS_DIR="$HOME/Downloads"' >>"${tmpFile}" || fail
           ^-- SC2016: Expressions don't expand in single quotes, use double quotes for that.


In vscode/vscode-lib.sh line 35:
      cat vscode/extensions.txt | while IFS="" read -r extension; do
          ^-------------------^ SC2002: Useless cat. Consider 'cmd < file | ..' or 'cmd file | ..' instead.

For more information:
  https://www.shellcheck.net/wiki/SC1090 -- Can't follow non-constant source....
  https://www.shellcheck.net/wiki/SC2034 -- i appears unused. Verify use (or ...
  https://www.shellcheck.net/wiki/SC2120 -- fail references arguments, but no...
